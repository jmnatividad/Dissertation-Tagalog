version: "3.9"

services:
  dissertation-ai:
    build:
      context: .
    container_name: dissertation_ai

    volumes:
      - .:/app

    env_file:
      - .env

    working_dir: /app

    stdin_open: true
    tty: true

    command: tail -f /dev/null